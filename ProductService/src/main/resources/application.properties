server.port=8090
logging.level.org.springframework.web: TRACE

spring.jpa.hibernate.ddl-auto=validate
# check if the schema as per code,
# is the exact same as the actual DB schema
# if not, throw error, or apply latest versions
spring.datasource.url=jdbc:mysql://localhost:3306/productserviceDB
spring.datasource.username=user10
#spring.datasource.password=ThePassword
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true